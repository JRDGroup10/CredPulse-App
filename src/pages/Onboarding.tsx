// Superseded by Auth.tsx once real Supabase accounts replaced the local-only
// demo profile form. Kept as a re-export so nothing breaks if anything still
// points at this file path.
export { default } from "./Auth";
